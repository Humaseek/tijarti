/**
 * @tijarti/shared-types
 *
 * Shared TypeScript types across web + mobile + any future surface.
 * This package has NO runtime dependencies — it's pure type definitions.
 *
 * Types defined here will be the source of truth for:
 *   - The data model (Transaction, Customer, Expense, etc.)
 *   - API contracts (once we add Supabase)
 *   - Zod schemas (later)
 *
 * Currently a placeholder; types will migrate here from apps/web/src/lib/store/types.ts
 * during the data-layer refactor.
 */

export {};
